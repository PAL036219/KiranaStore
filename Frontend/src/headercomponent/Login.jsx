import { Link } from "react-router-dom";

const Login = ({ className = "" }) => {
  return (
    <div className={`bg-blue-500  text-white px-9 py-3 rounded ${className}`}>
      Login
    </div>
  );
};

export default Login;
